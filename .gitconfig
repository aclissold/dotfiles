[user]
    name = Andrew Clissold
[credential]
    helper = cache --timeout=3600
[core]
    autocrlf = input
    excludesfile = ~/.gitignore
[alias]
    home = config user.email "andrewclissold@gmail.com"
    work = config user.email "andrew.clissold@dominos.com"
[color]
    ui = true
[push]
    default = simple
[merge]
    tool = vimdiff
    defaultToUpstream = true
